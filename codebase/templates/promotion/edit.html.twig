{% extends "base.html.twig" %}

{% block body %}
    <h1>Edit Promotion #{{ entity.id }} for item #<a href="{{ url('app_item_show', {id: entity.item.id}) }}">{{ entity.item.id }}</a></h1>
    {{ form(form) }}

{% endblock %}